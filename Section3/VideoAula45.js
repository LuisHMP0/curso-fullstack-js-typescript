// (condicção) ? 'Valor para verdadeiro' : 'Valor para falso';  
const pontuacaoUsuario = 900;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuário VIP' : 'Usuário normal';
// Basicamente                      if is true:           else: 
console.log(nivelUsuario);

